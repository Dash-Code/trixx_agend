class UserModel {
  int id;
  String nick;
  DateTime birthdate;
  String role;
  String orientation;
  String fetish;

  UserModel({this.id, this.nick, this.birthdate, this.role, this.orientation, this.fetish});
}